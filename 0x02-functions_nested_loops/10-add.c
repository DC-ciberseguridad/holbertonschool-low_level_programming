#include "holberton.h"

/**
 * add - Adds two integers and returns the result
 *@x: variable number being added
 *@y: variable number being added
 * Return: Always 0.
 */

int add(int n1, int n2);
{
	return (n1 + n2);
}
